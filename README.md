# File Integrity Checker

A tool that detects whether a file has been modified by comparing cryptographic fingerprints (hashes) of its contents over time.

## Overview

This project generates a unique fingerprint for a file, saves it, and later checks whether that fingerprint still matches.

## Tools Used

## Process

Work in Progress
